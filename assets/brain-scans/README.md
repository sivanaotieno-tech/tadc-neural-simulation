# Brain scan sources

Expected browser assets:

- `human-brain.webp` — supplied human brain scan/reference image.
- `bubble-fruit-fly-brain.jpg` — supplied fruit-fly brain reference used for Bubble.

The actual uploaded source files should be copied here with those names when publishing the binary project assets.

The MRI-derived 3D brain is generated separately from `bma-1-mri.nii.gz` and lives under `assets/models/`.
